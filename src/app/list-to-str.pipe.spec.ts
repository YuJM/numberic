import { ListToStrPipe } from './list-to-str.pipe';

describe('ListToStrPipe', () => {
  it('create an instance', () => {
    const pipe = new ListToStrPipe();
    expect(pipe).toBeTruthy();
  });
});
